# Krok 0 — SETUP (raz na klienta)

- Klient nie ma CLAUDE.md w swoim folderze → `/init-klienta <folder>` (żywy dokument
  JAK: błędy, struktura, architektura).
- Nie istnieje `docs/agents/issue-tracker.md` → `/setup-matt-pocock-skills`.
  Rekomendacje: tracker = **local markdown**; domain docs (CONTEXT.md, ADR-y) = folder
  klienta zgodnie z jego CLAUDE.md.
- W folderze klienta leży zastany plik analizy technicznej (artefakt pipeline'u
  sprzedażowego)? → rozdziel jego treść wg przeznaczenia, NIE wklejaj w całości nigdzie:
  LINKI → wiersze sekcji "🔗 Linki zewnętrzne" CLAUDE.md (jedno miejsce, dedup z tym
  co już tam jest) · trwałe reguły techniczne → "🧠 Wzorce techniczne"/"🏗️ Architektura"
  (tylko reguły, nie narracja) · opis stanu procesu → raport analizy (plik roboczy).
  Sam plik → archiwum klienta W CAŁOŚCI (nic nie ginie — w archiwum pozostaje czytelny).
  Pod tym workflow pliku analizy technicznej NIE tworzymy ani nie aktualizujemy.

Skonfigurowane → pomiń ten krok na zawsze.
